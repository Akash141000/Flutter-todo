const login = "login";

const register = "register";

const auth = "auth";

const todoItemShell = "todoItem";

const profileShell = "profile";

const todoListShell = "todoList";

String createRoutePath(String routeName) {
  return "/$routeName";
}
